{
  "name": "focus-jev",
  "version": "1.4.2",
  "private": true,
  "description": "A focused Chromium extension that uses JEV to keep browsing aligned with one session goal.",
  "scripts": {
    "lint": "node --check extension/core.js && node --check extension/background.js && node --check extension/guard.js && node --check extension/popup.js && node --check extension/options.js && node --check extension/blocked.js && node --check worker/src/index.mjs",
    "test": "node tests/test_core.js && node tests/test_extension_background.js && node tests/test_guard.js && node tests/test_guard_efficiency.js && node tests/test_ui_contract.js && node tests/test_gateway.mjs",
    "check": "npm run lint && npm test",
    "package": "npm run package:chrome && npm run package:firefox",
    "package:chrome": "sh scripts/package.sh",
    "package:firefox": "node scripts/package-firefox.mjs",
    "profile": "node scripts/profile.mjs",
    "worker:dev": "wrangler dev --config worker/wrangler.jsonc",
    "worker:deploy": "wrangler deploy --config worker/wrangler.jsonc",
    "worker:types": "wrangler types --config worker/wrangler.jsonc"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^8.1.0",
    "wrangler": "^4.135.0"
  }
}
